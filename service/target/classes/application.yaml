server:
  port: 8080

spring:
  profiles:
    active: dev
  main:
    allow-circular-references: true
  datasource:
      driver-class-name: ${sky.datasource.driver-class-name}
      url: jdbc:mysql://${sky.datasource.host}:${sky.datasource.port}/${sky.datasource.database}?serverTimezone=Asia/Shanghai&useUnicode=true&characterEncoding=utf-8&zeroDateTimeBehavior=convertToNull&useSSL=false&allowPublicKeyRetrieval=true
      username: ${sky.datasource.username}
      password: ${sky.datasource.password}

pagehelper:
  propertyName: propertyValue
  reasonable: false
  defaultCount: true # 分页插件默认参数支持 default-count 形式，自定义扩展的参数，必须大小写一致

#mybatis:
#  #mapper配置文件
#  mapper-locations: classpath:mapper/*.xml
#  type-aliases-package: com.zone.entity
#  type-handlers-package: com.zone.handler
#  configuration:
#    #开启驼峰命名
#   map-underscore-to-camel-case: true


logging:
  level:
    com:
      zone:
        mapper: debug
        service: debug
        controller: debug

zone:
  jwt:
    # 设置jwt签名加密时使用的秘钥
    admin-secret-key: zone
    # 设置jwt过期时间
    admin-ttl: 7200000000000
    # 设置前端传递过来的令牌名称
    admin-token-name: token


mybatis-plus:
  type-aliases-package: com.zone.entity#别名扫描包
  global-config: #Mapper.xml文件地址,默认值
    db-config:
      id-type: auto  #id为自增长
      logic-delete-field: deleted # 配置逻辑删除字段
  configuration:
    default-enum-type-handler: com.baomidou.mybatisplus.core.handlers.MybatisEnumTypeHandler
    cache-enabled: false #是否开启二级缓存


