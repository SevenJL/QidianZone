<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.zone.mapper.UserMapper">


    <insert id="insert" parameterType="com.zone.entity.User" keyProperty="userId">
        INSERT INTO user (password, user_name, email) VALUES (#{password}, #{userName}, #{email})
    </insert>
    <select id="pageQuery" resultType="com.zone.entity.User">
        SELECT * FROM user
        <where>
            <if test="userName != null and userName != ''">
                user_name LIKE concat('%', #{userName}, '%')
            </if>
            <if test="email != null and email != ''">
                AND email LIKE concat('%', #{email}, '%')
            </if>
        </where>
        ORDER BY user_id
        LIMIT #{page}, #{pageSize}
    </select>
</mapper>